<Sedimentation_Maps type = "ramp">
  <Event time = "0." >
    <Uniform materialFlag = "1" value= "0.3e-2"/>
  </Event>

  <Event time = "400." >
    <Uniform materialFlag = "2" value= "0.3e-2"/>
  </Event>

  <Event time = "800." >
    <Linear materialFlag = "3" value= "0.e0, 0.e0, 0.3e-2, 0.3e-2"/>
  </Event>
</Sedimentation_Maps>

<!--
<Sedimentation_Maps type="XXXX">

Block used to define the spatial and temporal distribution of sediment
deposition rates within the simulation domain.

The spatial distribution of sedimentation is applied exclusively to the
top surface of the domain and is specified through <Event> blocks.

The required attribute "type" defines the temporal behavior of sedimentation
between consecutive events.

Allowed values for "type":

  ramp
    Each <Event> block defines a new sedimentation map. The map remains
    unchanged until the next event occurs. In this mode, the deposition
    rate may vary spatially, but remains constant in time between two
    consecutive events.

  linear
    Between two consecutive <Event> blocks, referred to as event A and
    event B, the sedimentation map evolves linearly in time. The deposition
    rate values are interpolated between those defined in event A and those
    specified in event B.


<Event time="XXX">

Each <Event> block defines a new sedimentation map applied to the top
surface of the domain.

Attributes:

  time
    Time associated with the sedimentation map defined by the event.
    The value must be specified as a real number.

Allowed child blocks:

  <Uniform>
  <Linear>
  <Map>

These blocks may be used simultaneously and in any combination.


<Uniform materialFlag="X" value="XXXX"/>

Defines a uniform sedimentation rate over the entire top surface of the
domain for a given material.

Attributes:

  materialFlag
    Identifier of the material to which sedimentation is applied.
    This value must correspond to the cellTags attribute defined in the
    material declaration:

      <Solid name="XXXX" cellTags="X">

    If no material corresponding to the specified value exists, the block
    is ignored.

  value
    Uniform sedimentation rate applied throughout the domain for the
    specified material.


<Linear materialFlag="X" value="XXX, XXX, XXX, XXX"/>

Defines a spatially varying sedimentation rate through bilinear
interpolation over the top surface of the domain.

Attributes:

  materialFlag
    Identifier of the material to which sedimentation is applied.
    This value must correspond to the cellTags attribute defined in the
    material declaration:

      <Solid name="XXXX" cellTags="X">

    If no material corresponding to the specified value exists, the block
    is ignored.

  value
    Set of four values representing the sedimentation rate at the four
    corner vertices of the domain top surface.

    The vertex numbering convention is:

      4 -------- 3
      |          |
      |          |  y
      |          |  |
      1 -------- 2  +---- x

    Values must be specified in the following order:

      value = "v1, v2, v3, v4"

    where v1, v2, v3, and v4 correspond to the sedimentation rates at
    vertices 1, 2, 3, and 4, respectively.


<Map materialFlag="X"
     division="NX,NY"
     file="xxxx.dat"/>

Defines the sedimentation rate on the top surface of the domain from
a discrete map of values.

Attributes:

  materialFlag
    Identifier of the material to which sedimentation is applied.
    This value must correspond to the cellTags attribute defined in the
    material declaration:

      <Solid name="XXXX" cellTags="X">

    If no material corresponding to the specified value exists, the block
    is ignored.

  division
    Number of points used to discretize the sedimentation map along the
    directions of the domain top surface.

    If these values match the discretization used by the solver, each
    sedimentation value is directly assigned to a domain column.

    Otherwise, a linear interpolation is performed between the input map
    and the discretization effectively used in the simulation.

  file
    Path to the file containing the sedimentation map values.

    The file must contain exactly NX × NY values, where NX and NY are
    the dimensions specified by the division attribute.

-->